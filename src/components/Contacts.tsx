import React from "react";

const Contacts = () => {
  return (
    <section id="contacts">
      <div className="container">
        <h2>Контакты</h2>
        <div className="card"></div>
      </div>
    </section>
  );
};

export default Contacts;
